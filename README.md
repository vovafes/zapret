<div align="center">

# ⚡ zapret-discord

**Локальный инструмент обхода DPI-блокировок для Discord**

Работает на уровне ядра Windows · Управляется из облака · Без VPN-серверов

[![Windows](https://img.shields.io/badge/Windows-10%2F11%20x64-0078D6?style=flat-square&logo=windows&logoColor=white)](https://github.com/vovafes/zapret/releases)
[![Python](https://img.shields.io/badge/Python-3.10%2B-3776AB?style=flat-square&logo=python&logoColor=white)](https://www.python.org/)
[![License](https://img.shields.io/badge/License-MIT-22c55e?style=flat-square)](LICENSE)
[![Releases](https://img.shields.io/github/v/release/vovafes/zapret?style=flat-square&label=Версия&color=22c55e)](https://github.com/vovafes/zapret/releases)

</div>

---

## 🆕 Что нового в v2.1.0

- **Исправлена загрузка фото.** `media.discordapp.net` (домен, через который клиент грузит превью и вложения-изображения) не входил в `target_domains`, из-за чего эти соединения не десинхронизировались и блокировались ТСПУ отдельно от текста и голоса. Добавлен `discordapp.net` — подстрочное совпадение покрывает все поддомены (`media.`, `images-ext-*.` и т.д.).
- **Список доменов расширен под другие заблокированные в СНГ сервисы** — тот же механизм фрагментации `TLS ClientHello` работает для любого HTTPS-сервиса, попавшего под SNI-блокировку по ТСПУ, не только для Discord. Добавлены: YouTube (`googlevideo.com`, `ytimg.com`, `ggpht.com`), Instagram/Facebook (`fbcdn.net`, `facebook.com`), X/Twitter (`x.com`, `twimg.com`), WhatsApp, Signal, LinkedIn, Viber, Snapchat, Twitch, Roblox и TikTok. Полный актуальный список — в [`config.json`](https://github.com/vovafes/zapret/blob/main/config.json).

> ⚠️ Список не панацея: часть перечисленных сервисов (например, Facebook/Instagram как продукты Meta, X) в некоторых странах СНГ блокируется не только по SNI, но и по IP/DNS — фрагментация `ClientHello` тут не поможет без дополнительного обхода DNS-блокировки (смена DNS-сервера или DoH). Если после обновления сервис всё ещё недоступен — сначала проверьте, резолвится ли домен вообще.

> Изменения применяются автоматически при следующем запуске программы через облачный конфиг — если вы правите `config.json` локально для сборки из исходников, синхронизируйте эти домены вручную.

## 🆕 Что нового в v2.0.0

Провайдеры обновили ТСПУ: старый способ обхода (разбить пакет ровно на 2 части) в 2026 году перестал стабильно работать, потому что фильтр теперь сначала собирает весь поток и только потом его проверяет. Новая версия обходит это:

- пакет режется на **случайное число частей** вместо двух фиксированных,
- перед настоящими данными отправляется **поддельный пакет-приманка**, который сбивает фильтр со следа и не долетает до сервера Discord.

Всё это настраивается автоматически через облачный конфиг — обновлять программу вручную не нужно, достаточно перезапустить её.

---

## О проекте

**zapret-discord** — лёгкая утилита с открытым исходным кодом для Windows, которая восстанавливает работу Discord в условиях ограничений провайдеров, использующих системы **ТСПУ** (Технические Средства Противодействия Угрозам) / **DPI** (Deep Packet Inspection), без использования сторонних VPN-серверов и без изменения системных DNS-настроек.

Принцип работы основан на **локальной десинхронизации TCP-пакетов**: программа перехватывает исходящий TLS-хэндшейк на уровне ядра через драйвер **WinDivert** и разбивает пакет `TLS ClientHello` на несколько фрагментов так, что система DPI провайдера не успевает прочитать поле **SNI** (Server Name Indication) и применить блокировку — при этом сервер Discord корректно собирает фрагменты и устанавливает полноценное соединение.

**Главная особенность** — **OTA-обновление конфигурации**: параметры работы хранятся в файле [`config.json`](https://github.com/vovafes/zapret/blob/main/config.json) прямо в этом репозитории. Это позволяет адаптировать обход под новые методы блокировки без переустановки и пересборки программы у всех пользователей одновременно.

---

## ✨ Ключевые особенности

| Функция | Описание |
|---|---|
| ☁️ **Облачная конфигурация (OTA)** | При каждом запуске программа синхронизирует параметры с `vovafes/zapret` на GitHub. Автор может менять метод обхода для всех пользователей разом, обновив один файл `config.json`. |
| 🔑 **Автоповышение прав (UAC)** | При запуске без прав администратора программа автоматически показывает UAC-запрос через `ShellExecuteW`. Не нужно вручную выбирать «Запуск от имени администратора». |
| 📡 **Обход ТСПУ для текста и медиа** | Фрагментация `TLS ClientHello` обходит DPI-фильтры для Discord API, CDN и Gateway. |
| 🎙️ **Фикс голосовых и видеоканалов** | Специальный UDP-режим обфускации восстанавливает работу голосовых чатов и видеозвонков (UDP-порты 50000–65535). |
| 🚫 **Без сторонних VPN-серверов** | Весь трафик идёт напрямую к серверам Discord. Нет задержек туннеля, нет утечек данных. |
| 🛠️ **Многосегментная десинхронизация + фейк-пакеты** | `fake_multisplit` — случайное число фрагментов ClientHello в случайных позициях, отправляемых вперемешку, плюс поддельные ClientHello с заниженным TTL для сброса трекинга DPI. Legacy-режимы `split`/`disorder` (фикс. разбиение на 2 части) оставлены для отката. |
| ⚡ **Минимальное влияние на производительность** | Фильтруются только пакеты на порт 443 (TCP) и порты 50000–65535 (UDP). Остальной трафик проходит без задержек. |

---

## Системные требования

| Компонент | Требование |
|---|---|
| **Операционная система** | Windows 10 или Windows 11 (только 64-бит) |
| **Права** | Администратор (запрашиваются автоматически) |
| **Драйвер** | `WinDivert.dll` + `WinDivert64.sys` (входят в архив релиза) |
| **Сеть** | Доступ к GitHub для синхронизации конфига (необязателен) |
| **Python** | 3.10+ (только при запуске из исходников) |

> ⚠️ Windows 7, 8, 8.1 и 32-битные версии Windows **не поддерживаются**.

---

## 🚀 Быстрый старт (для обычных пользователей)

> Этот способ **не требует** установки Python или каких-либо технических навыков.

### Шаг 1 — Скачайте архив

Перейдите на страницу [**Releases**](https://github.com/vovafes/zapret/releases) и скачайте последний архив `zapret-discord-vX.X.X.zip`.

### Шаг 2 — Распакуйте в любую папку

После распаковки убедитесь, что в папке присутствуют **все три файла**:

```
📁 zapret-discord\
  ├── zapret-discord.exe    ← основной исполняемый файл
  ├── WinDivert.dll         ← обязательно!
  └── WinDivert64.sys       ← обязательно!
```

> ⛔ **Критически важно:** `WinDivert.dll` и `WinDivert64.sys` **обязаны** находиться в той же папке, что и `zapret-discord.exe`. Без них программа не запустится.

### Шаг 3 — Запустите

Дважды кликните по `zapret-discord.exe`. Появится системный запрос **Контроля учётных записей (UAC)** — нажмите **«Да»**.

Программа автоматически:
1. Запросит права администратора
2. Загрузит актуальный конфиг с репозитория `vovafes/zapret`
3. Активирует перехват трафика Discord

**Оставьте окно программы открытым.** Для корректной остановки нажмите **Ctrl+C**.

---

## 🛠️ Сборка из исходников (для разработчиков)

### 1. Клонирование репозитория

```bash
git clone https://github.com/vovafes/zapret.git
cd zapret
```

### 2. Установка зависимостей

```bash
pip install pydivert
```

### 3. Запуск напрямую

```bash
python main.py
```

### 4. Сборка автономного `.exe`

```bash
pip install pyinstaller
pyinstaller --onefile --uac-admin --name zapret-discord --clean main.py
```

Готовый файл появится в `dist\zapret-discord.exe`. Скопируйте `WinDivert.dll` и `WinDivert64.sys` рядом с ним.

---

## ⚙️ Конфигурация (`config.json`)

> 💡 **Обычному пользователю трогать этот файл не нужно.** Программа сама скачивает актуальные настройки при каждом запуске — обход подстраивается под изменения блокировок автоматически. Этот раздел — для тех, кто хочет донастроить обход вручную (например, под конкретного провайдера) или собирает программу из исходников.

При каждом запуске программа загружает конфигурацию с:

```
https://raw.githubusercontent.com/vovafes/zapret/main/config.json
```

Если GitHub недоступен, программа использует встроенную конфигурацию по умолчанию.

### Структура

```json
{
  "target_domains": [
    "discord.com",
    "discordapp.com",
    "discordapp.net",
    "gateway.discord.gg",
    "discord.media",
    "cdn.discordapp.com",
    "youtube.com",
    "youtube-nocookie.com",
    "googlevideo.com",
    "ytimg.com",
    "ggpht.com",
    "instagram.com",
    "cdninstagram.com",
    "fbcdn.net",
    "facebook.com",
    "fb.com",
    "twitter.com",
    "x.com",
    "twimg.com",
    "whatsapp.com",
    "whatsapp.net",
    "signal.org",
    "whispersystems.org",
    "linkedin.com",
    "licdn.com",
    "viber.com",
    "snapchat.com",
    "sc-cdn.net",
    "twitch.tv",
    "ttvnw.net",
    "jtvnw.net",
    "roblox.com",
    "rbxcdn.com",
    "tiktok.com",
    "tiktokcdn.com",
    "tiktokv.com",
    "musical.ly"
  ],
  "desync_mode": "fake_multisplit",
  "split_position": 2,
  "fragment_count_min": 3,
  "fragment_count_max": 7,
  "randomize_fragments": true,
  "shuffle_fragment_order": true,
  "fake_packet_enabled": true,
  "fake_packet_count": 1,
  "fake_packet_ttl": 4,
  "udp_fake_enabled": true
}
```

| Параметр | Тип | По умолчанию | Описание |
|---|---|---|---|
| `target_domains` | `string[]` | — | Домены для перехвата (совпадение — по подстроке в SNI, поэтому `"discordapp.net"` уже покрывает `media.discordapp.net`, `images-ext-1.discordapp.net` и т.д.) |
| `desync_mode` | `string` | `"fake_multisplit"` | `"fake_multisplit"`, `"multisplit"`, `"split"` (legacy) или `"disorder"` (legacy) |
| `split_position` | `integer` | `2` | Позиция разбивки — используется только в legacy-режимах |
| `fragment_count_min` / `fragment_count_max` | `integer` | `3` / `7` | Диапазон числа фрагментов ClientHello в режимах `multisplit`/`fake_multisplit` |
| `randomize_fragments` | `boolean` | `true` | Случайные позиции разрыва при каждом ClientHello (не фиксированный паттерн) |
| `shuffle_fragment_order` | `boolean` | `true` | Отправлять фрагменты не по порядку |
| `fake_packet_enabled` | `boolean` | `true` | Слать поддельный ClientHello перед реальным (TTL-trick) |
| `fake_packet_count` | `integer` | `1` | Сколько фейковых пакетов слать |
| `fake_packet_ttl` | `integer` | `4` | TTL фейка — должен быть меньше числа хопов до сервера Discord, но достаточным, чтобы пакет прошёл локальный ТСПУ |
| `udp_fake_enabled` | `boolean` | `true` | UDP-обфускация для голосовых каналов |

---

## 🔬 Как это работает (Архитектура)

```
 ┌──────────────────────────────────────────────────────────────────┐
 │                Discord App (браузер / клиент)                    │
 │    Исходящий пакет: TCP → TLS ClientHello (SNI: discord.com)     │
 └────────────────────────────┬─────────────────────────────────────┘
                              │  (пакет перехвачен до отправки)
                              ▼
 ┌──────────────────────────────────────────────────────────────────┐
 │                     zapret-discord                               │
 │              (WinDivert — уровень ядра Windows)                  │
 │                                                                  │
 │  1. Перехват исходящего TCP-пакета на порт 443                   │
 │  2. Обнаружение TLS Handshake: byte[0]=0x16, byte[5]=0x01        │
 │  3. Поиск SNI в payload: "discord.com" найден ✓                  │
 │  4. Отправка поддельного ClientHello (TTL занижен, домен-приманка)│
 │  5. Фрагментация payload на N случайных частей                   │
 │  6. Отправка фрагментов вперемешку с корректными TCP SEQ         │
 └───────────────┬────────────────────────┬────────────────────────┘
                 │ Фейк (гаснет по TTL)   │ Фрагменты ①…N вперемешку
                 ▼                        ▼
 ┌──────────────────────────────────────────────────────────────────┐
 │                  ТСПУ / DPI провайдера                           │
 │  Фейк сбивает трекинг потока; фрагменты приходят без             │
 │  устойчивого паттерна разрыва — DPI не может опознать поток      │
 │  Правило блокировки не срабатывает → фрагменты пропущены ✓       │
 └───────────────────────────────┬──────────────────────────────────┘
                                 ▼
 ┌──────────────────────────────────────────────────────────────────┐
 │                    Сервер Discord                                │
 │  TCP-стек собирает фрагменты по SEQ-номерам                      │
 │  Полный ClientHello → TLS Handshake завершён → соединение ✓      │
 └──────────────────────────────────────────────────────────────────┘
```

### Детали реализации

**WinDivert** встраивается в сетевой стек Windows (WFP/NDIS) и перехватывает пакеты до их передачи адаптеру. Это локальная операция — трафик не покидает компьютер через посредника.

**TLS ClientHello** определяется по сигнатуре: `byte[0] == 0x16` (Handshake) и `byte[5] == 0x01` (ClientHello). Расширение SNI с именем домена содержится в пакете в открытом виде.

**Режим Fake+Multisplit (по умолчанию):** современный ТСПУ буферизует до ~16 начальных пакетов потока и пересобирает их перед анализом, поэтому разбиение ровно на 2 фиксированные части (см. legacy-режимы ниже) он переживает без проблем. Вместо этого:
1. Перед реальными данными отправляется `fake_packet_count` поддельных ClientHello — тот же TLS-заголовок, но случайный payload с доменом-приманкой вместо `discord.com`, и заниженный TTL (`fake_packet_ttl`). Пакет физически не долетает до сервера Discord (гарантированно истекает в пути), но виден локальному ТСПУ и засоряет его состояние по потоку.
2. Настоящий ClientHello режется на случайное (`fragment_count_min`–`fragment_count_max`) число фрагментов в случайных позициях и отправляется в перемешанном порядке — сигнатурный паттерн разрыва не повторяется от соединения к соединению.

**Legacy-режимы Split/Disorder:** фиксированное разбиение на 2 части (`split_position`) — оставлены в коде для отката, если `fake_multisplit` даст сбой на конкретном провайдере.

**UDP-обфускация:** перед каждым голосовым пакетом отправляется короткий фиктивный пакет, сбрасывающий состояние UDP-трекера DPI.

**OTA-конфигурация:** параметры хранятся в `config.json` в репозитории `vovafes/zapret`. При изменении методов блокировки достаточно обновить этот файл — все пользователи получат новые настройки при следующем запуске.

---

## 🛡️ Антивирусы и ложные срабатывания

Программы, работающие с сетевым трафиком на уровне ядра через WinDivert, могут вызывать ложные срабатывания антивирусного ПО. Это ожидаемое поведение — программа не содержит вредоносного кода.

Если антивирус блокирует запуск, добавьте папку с программой в исключения.

---

## ❓ Частые вопросы

**Q: Discord всё равно не подключается.**  
A: Попробуйте увеличить `"fragment_count_max"` (например до 10) и `"fake_packet_count"` (до 2-3) в `config.json`. Если и это не помогает — откатитесь на `"desync_mode": "split"` или `"disorder"`, некоторые провайдеры реагируют на многосегментность иначе.

**Q: Голосовые каналы не работают.**  
A: Убедитесь, что `"udp_fake_enabled": true`. В настройках Discord попробуйте сменить голосовой регион сервера.

**Q: Программа закрылась — интернет работает нормально?**  
A: Да. При нажатии Ctrl+C программа корректно закрывает хэндл WinDivert и весь трафик снова идёт напрямую без обработки.

**Q: Работает ли на Windows 7 / 32-бит?**  
A: Нет. Только Windows 10 и 11 x64.

---

## ⚖️ Важное правовое уведомление

> Данный проект создан **исключительно в образовательных целях** и для обеспечения доступа к легитимным инструментам коммуникации.
>
> Программа **не собирает, не записывает и не передаёт** никакие пользовательские данные, содержимое переписки или личную информацию. Весь перехватываемый трафик обрабатывается локально и передаётся напрямую на серверы Discord.
>
> **Discord** является торговой маркой Discord Inc. Данный проект не связан с Discord Inc.

---

<div align="center">

Сделано для русскоязычного сообщества Discord

[📦 Релизы](https://github.com/vovafes/zapret/releases) · [🐛 Сообщить об ошибке](https://github.com/vovafes/zapret/issues) · [⚙️ config.json](https://github.com/vovafes/zapret/blob/main/config.json)

</div>
